import type { Language } from './model/language'

/**
 * List of languages known along with fluency levels.
 *
 * Each language entry includes the language name and the proficiency level (e.g., native, intermediate).
 */
export const Languages: Array<Language> = [
  { language: 'Español', fluency: 'Nativo' },
  { language: 'English', fluency: 'Intermediate/B2' }
]
